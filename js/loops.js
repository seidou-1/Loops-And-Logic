for (var x =1; x<10; x++){
  console.log(x);
}

//While loop:
var y = 7;

while (y>0) {
  console.log(y);
  y--;
  //The conditional must at some point stop so there must be something that happens to the variable each time
}
